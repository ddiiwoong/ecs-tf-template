version: 2.1
jobs:
  test:
    working_directory: ~/project
    docker:
      - image: hashicorp/terraform:0.11.14
    steps:
      - checkout
      - run: 
          name: "Setup AWS environment variables"
          command: |
            echo 'export AWS_ACCESS_KEY_ID="$AWS_ACCESS_KEY_ID"' >> $BASH_ENV
            echo 'export AWS_DEFAULT_REGION="$AWS_DEFAULT_REGION"' >> $BASH_ENV
            echo 'export AWS_SECRET_ACCESS_KEY="$AWS_SECRET_ACCESS_KEY"' >> $BASH_ENV
            echo 'export TF_VAR_AWS_ACCESS_KEY_ID="$TF_VAR_AWS_ACCESS_KEY_ID"' >> $BASH_ENV
            echo 'export TF_VAR_AWS_SECRET_ACCESS_KEY="$TF_VAR_AWS_SECRET_ACCESS_KEY"' >> $BASH_ENV
      - run:
          name: Init terraform 
          command: terraform init ~/project/ecs-provision-tf
      - run:
          name: Validate terraform 
          command: terraform validate ~/project/ecs-provision-tf
      - run:
          name: Plan terraform
          command: terraform plan ~/project/ecs-provision-tf
  deploy:
    working_directory: ~/project
    docker:
      - image: hashicorp/terraform:0.11.14
    steps:
      - checkout
      - run:
          name: Init terraform
          command: terraform init ~/project/ecs-provision-tf
      - run:
          name: Apply terraform
          command: terraform apply -auto-approve ~/project/ecs-provision-tf
workflows:
  version: 2
  test-and-deploy:
    jobs:
      - test
      - hold:
          type: approval
          requires:
            - test
          filters:
            branches:
              only: master
      - deploy:
          requires:
            - hold
          filters:
            branches:
              only: master
